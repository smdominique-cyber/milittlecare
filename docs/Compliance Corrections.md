Phase 1 
Task: Compliance Engine — CDC-layer correctness pass. Continue on the EXISTING branch feature/h1-cdc-gating (current HEAD a0ed236, which already has the H1 CDC-gating work — that work stays, this pass builds on it). Do NOT merge — halt for live gate. This is registry + doc work; no migration.
Context: A verification pass against the authoritative 2026 sources (the Family/Group Home TA Manual, the CCL-3900, the CDC Scholarship Handbook for Licensed Providers rev. 1/1/2026, and the LEP guidance) found the CDC/subsidy layer needs corrections. The H1 gating you already did is correct in shape but has a §2a flaw (below) that must be fixed before this merges. Read src/lib/complianceState.js (the CDC rows: funding_dhs_198_on_file G1, funding_enrollment_agreement_on_file G2, cdc_authorization_currency G3, cdc_fingerprint_reprint_currency G4, provider_miregistry_annual_ongoing F1, provider_miregistry_level_2_currency F2, attendance_parent_acknowledgment_per_day H1) and their tests before editing.
1 — §2a THREE-WAY FIX across G2, G3, H1 (the load-bearing change).
Your H1 gating (and the existing G2/G3) treats empty funding_sources as does_not_apply. That conflates two different situations and violates §2a. Fix all three rows to distinguish:

?1 active CDC funding source present ? APPLIES
funding data loaded AND genuinely contains no CDC source (e.g. private-pay-only) ? DOES_NOT_APPLY (legitimate — provider has affirmatively no CDC kids)
funding data absent / not loaded / signal unavailable ? UNKNOWN (NEVER silent does_not_apply)

The distinction between cases 2 and 3 is the crux: an empty funding_sources array because the provider has none is different from an empty array because the loader hasn't returned or the data isn't available to the resolver. You must determine whether the source layer can actually distinguish "loaded-and-empty" from "absent/unloaded" — check how funding_sources reaches the resolver (the loader in complianceStateLoader.js and what it passes as sourceRows). If the loader can signal "loaded successfully, zero rows" vs "not loaded / failed," use that to drive the does_not_apply-vs-unknown split. If it currently CANNOT distinguish them (i.e. both look like []), STOP and report that — because then the §2a fix requires a loader change to carry that signal, and we need to decide that deliberately rather than guess. Do not fake the distinction by treating [] as unknown always (that would wrongly flag private-pay providers) or as does_not_apply always (the current bug).
Apply the corrected three-way to G2, G3, and H1 (G4 already does belt-and-suspenders LEP+CDC gating per the prior audit — confirm it's §2a-clean too and align it if not). This is the single most important part of this pass; gate it carefully.
2 — REMOVE G1 (funding_dhs_198_on_file) entirely.
The DHS-198 is an authorization notice MDHHS sends to the provider (showing authorized hours/dates), not a document the provider fills out, signs, or uploads. It's an input the provider receives, not a compliance obligation they fulfill — so it does not belong as a checklist row. Delete the registry row and its tests. Grep for any other references (funding_dhs_198_on_file, "DHS-198" in the worksheet/scope docs/guidance maps) and remove/adjust them so nothing dangles.
3 — F1 (provider_miregistry_annual_ongoing) — wording nuance.
The Dec 16 deadline and the "account is closed" consequence are CONFIRMED correct (LEP CDC providers must complete the Michigan Ongoing Health & Safety Training Refresher by Dec 16; failure ? account closed; re-enrollment requires completing the current year's training). Keep that. ADD the nuance: the obligation begins the calendar year AFTER enrollment/reenrollment — a brand-new LEP is not subject to it in their first partial year. Adjust the applicability/copy so a first-year LEP doesn't see it as a current gap. Keep the citation as the CDC/LEP handbook (not R 400).
4 — F2 (provider_miregistry_level_2_currency) — reframe from compliance-gap to pay-rate.
Level 2 is OPTIONAL — it's a higher-pay tier the LEP earns by completing 10 approved training hours/year, NOT a compliance requirement they fall out of into violation. When Level 2 "expires," the consequence is the provider drops to the Level 1 (base) pay rate — it is NOT a compliance delinquency or account closure. Reframe this row's copy accordingly: it's a "your pay rate will drop / you're leaving money on the table" informational alert, not a red compliance gap. If the engine's state-kind vocabulary can express "informational / advisory" vs "delinquent," use the advisory framing; if not, note how it currently renders and flag whether a softer state is warranted (don't invent a new state kind without flagging).
5 — G3 (cdc_authorization_currency) — re-scope the guidance.
The term "redetermination" and the 12-month cycle are correct, BUT redetermination is the parent's responsibility (the parent submits documentation; eligibility is reviewed starting the 11th month). The provider does NOT "process redetermination." Reword guidance so it's accurate to the provider's actual role: the provider keeps the authorization end date current and is aware that billing stops when authorization lapses; the parent handles redetermination; the contact for authorization questions is MDHHS (844-464-3447). Keep the §2a three-way from Part 1.
6 — G4 (cdc_fingerprint_reprint_currency) — applicability correction.
The 5-year fingerprint reprint is NOT LEP-only. Per the CDC handbook, all providers/staff/household members fingerprinted prior to April 2024 need an additional background check based on fingerprints five years after initial — this applies to licensed Family/Group Home providers too, not just LEPs. Correct the applicability so licensed providers also get this row (the 5-year cycle is confirmed). Keep CDC-source gating where appropriate, but the reprint obligation itself is not LEP-exclusive.
7 — Docs.
Update the worksheet (docs/phase-3-1-guidance-review-worksheet.md) and scope doc for all the above: H1 re-cited + gated, G1 removed, F1 nuance, F2 reframe, G3 re-scope, G4 correction. Add a maintenance note: CDC-sourced citations (F1, F2, G2, G3, G4, H1) derive from the CDC Scholarship Handbook, which is revised quarterly (Jan 1 / Apr 1 / Jul 1 / Oct 1) — flag these rows as "verify each quarter." R 400 licensing citations are stable and don't need this. (Also note in the worksheet that H1's original "is this licensing?" question is now resolved: it's CDC billing audit-trail, not R 400.)
Constraints: registry + doc + test only, no migration, no UI component work (that's later passes). Keep the existing H1 gating logic; this pass corrects its §2a handling and aligns G2/G3, it doesn't redo it. If the loader genuinely can't distinguish loaded-empty from absent (Part 1), STOP and report before faking it. Suite green. Commit + push to feature/h1-cdc-gating. Do NOT merge.
Report: the §2a fix and whether the loader could distinguish the two empty cases (or whether that needs a loader change — flag it); confirmation G1 is fully removed with no dangling refs; the F1/F2/G3/G4 changes; doc updates; and the exact live-gate steps for Seth (especially: how to verify a private-pay-only provider sees the CDC rows as not_applicable/dormant, a CDC provider sees them live, and — if testable — what an "unloaded funding" state shows).

Phase 2
Task: SCOPE the loader-shape change that enables the §2a three-way (loaded-empty vs absent) — DO NOT BUILD. Produce a scope doc, commit on a docs branch, push, halt. The first half of this is an AUDIT, not a design: I need to know the blast radius before we design the fix.
Background: src/lib/complianceStateLoader.js safeQuery (lines ~43-56) returns [] for three cases the resolver can't distinguish: genuine empty result, PostgREST/RLS error, and thrown exception. This means every requirement resolver treats "data failed to load" identically to "data loaded, zero rows." For the CDC rows this causes a §2a violation (a transient load failure shows CDC requirements as not_applicable instead of unknown). We want to fix it so the loader carries a per-table loaded/failed signal, letting resolvers return unknown when data is genuinely absent/unloaded vs does_not_apply/missing when loaded-and-empty. But this is an engine-wide change, so scope before building.
PART 1 — BLAST-RADIUS AUDIT (do this first, report before designing).
Read complianceState.js and complianceStateLoader.js and enumerate:

Every sourceRows.<table> key the loader produces, and which requirement resolvers + applicability rules read each one.
For EACH requirement that reads a sourceRows table: what does it currently do when that table is []? Does empty mean not_applicable, missing_required, on_file, or something else for that row? This is the critical column — because if we make the loader distinguish loaded-empty from failed, every resolver that currently treats [] as a meaningful state could start seeing a "not loaded" signal it doesn't handle.
Flag which resolvers would need to change behavior under the new signal vs. which are safe (e.g. a row where empty legitimately means missing_required regardless of load state may not want to flip to unknown).
Identify the risk cases: requirements where "[] currently means X" but "[] could now mean unknown" would change what the provider sees. Especially: would any row that currently shows actionable gaps suddenly show unknown/"we couldn't check" on a normal first load while data is still arriving? (That would be a regression — turning working rows into "unknown" noise.)

STOP after Part 1 and report the audit table (requirement ? table(s) read ? current empty-behavior ? would-change-under-new-signal Y/N ? risk note) before proceeding to design. This audit determines whether the loader change is surgical (a few CDC rows) or sweeping (much of the registry). I want to see it before we commit to the design.
PART 2 — DESIGN (in the same doc, after the audit):
Propose the loader-shape change: the options (per-table { rows, loaded } vs a sibling loadedKeys: Set<string> vs another shape), with a recommendation. Address: how resolvers opt into the new signal (do they all have to change, or can the engine default unread tables to a safe behavior?); how to avoid the first-load-flicker regression (data legitimately mid-load shouldn't show as unknown if the UI gates on a loading state already — does it?); whether the §2a three-way applies to ALL tables or just the CDC funding-source one (maybe we only need the signal for funding_sources, which would massively shrink the change); and the test plan.
Key scoping question to answer in the doc: can we fix just funding_sources (the one table where the §2a violation actually bites, per the CDC-layer pass) rather than re-plumbing every table? A narrow fix that adds the loaded/failed signal for funding_sources only — leaving every other table's behavior untouched — might close the actual hole with a fraction of the blast radius. Evaluate that as the likely-preferred option.
Constraints: doc only, no code. Status "Scope — DRAFT." Report the Part 1 audit table prominently — that's the deliverable I care most about.

Phase 3
Task: Build the §2a loader-shape fix — close the silent-not_applicable-on-load-failure hole across all 12 affected rows. Per the scope doc docs/pr-compliance-loader-shape-scope.md (commit 792b815) — read it first; it has the full audit. Build on a NEW branch off main. Do NOT merge — halt for live gate. No migration expected (verify).
Decision locked by Seth: the BROAD fix — all 12 §2a-violating rows, not the narrow 4-row CDC-only version. Use Option B (the sibling sourceRowsLoaded signal, backward-compatible) per the scope doc's recommendation.
The 12 rows to fix (each currently produces a silent not_applicable when its source table is [], which is wrong when the [] is a load failure rather than genuine emptiness):

CDC group (4): G2 funding_enrollment_agreement_on_file, G3 cdc_authorization_currency, G4 cdc_fingerprint_reprint_currency, H1 attendance_parent_acknowledgment_per_day — all on funding_sources
Medication (6): D1–D6 — on medication_authorizations
Per-trip consents (2): C4 consent_transportation_nonroutine_per_trip_recency, C5 consent_water_activities_off_premises_per_trip_recency — on acks
Staff (1): E6 caregiver_health_safety_update_acked — on health_safety_updates

CRITICAL FIRST STEP — verify the pattern before applying it. The audit's summary lists all 12 as the same "data-inferred N/A on empty table" shape. Before rewriting anything, read each of the 12 rows' actual inferFromData/state_resolver and confirm each one truly does "empty table ? not_applicable" in the same way. If ANY of the 12 is shaped differently — does something subtler with the empty case, or its empty-means-N/A is actually correct-and-intended rather than a §2a violation — STOP and report that row before applying the uniform fix. Do not paper over a difference the audit's summary table smoothed over. Report which of the 12 share the exact pattern and which (if any) are exceptions.
The fix, per row: the three-way must be:

source table loaded AND contains a relevant row ? APPLIES (then the existing state logic runs)
source table loaded AND genuinely empty (no relevant rows) ? DOES_NOT_APPLY (the legitimate current behavior — preserve it)
source table NOT loaded / load failed / signal absent ? UNKNOWN (the §2a fix — never silent not_applicable)

The loader change (Option B): extend complianceStateLoader.js so it produces a sibling signal — sourceRowsLoaded (a per-table { table: true/false } or Set<string> of successfully-loaded tables) alongside sourceRows. safeQuery currently swallows all three cases to []; the loaded-signal must distinguish "query succeeded, zero rows" (loaded:true) from "query errored or threw" (loaded:false). Resolvers/applicability rules that need the §2a three-way read this signal; resolvers that DON'T read it must behave exactly as today (backward-compat is the safety property — the other ~21 "would change" rows from the audit must NOT change behavior because they don't opt in).
Flicker-safety: per the audit, both ComplianceChecklistPage and FamilyComplianceTab already gate on a loading state before calling the engine, so normal first-load won't show unknown. Put the loaded signal on the convenience-wrapper return envelope (the { state, children } shape) per scope decision #2, so a future non-page consumer inherits the ability to branch on load state rather than reintroducing the flicker. Confirm the existing pages still gate correctly and don't show a flash of unknown.
Tests: for each of the 12 rows, add the three-way test trio — loaded+present ? applies/normal-state, loaded+empty ? does_not_apply, not-loaded ? unknown. Plus loader-level tests that sourceRowsLoaded correctly reports true for empty-success and false for error/throw. Plus a backward-compat assertion that a representative non-opted-in row (one that produces missing_required from empty, e.g. one of the intake rows) still behaves exactly as before. Keep the suite green.
Constraints: no migration (confirm — this is engine + loader + tests). Don't touch the ~21 rows that "would change under the signal but don't violate §2a" — they stay on current behavior by not reading the new signal. If the loader change turns out to require touching something broader than complianceStateLoader.js + the 12 resolvers + the convenience wrapper, STOP and report scope creep before proceeding.
Report: the pattern-verification result (which of the 12 share the shape, any exceptions); the loader change; confirmation the 21 non-opted-in rows are untouched; the flicker check; and the live-gate steps for Seth — specifically how to exercise the load-failure path (the hard part to test: how does Seth simulate "funding_sources failed to load" to confirm the CDC rows show unknown not not_applicable? If it can't be simulated in-browser, say so and note what the unit tests cover instead).**