Here's the scope-doc prompt. Written tight, central decision locked, verbatim rule text included so CC isn't working from paraphrase.

Task: Draft docs/pr-consents-C-scope.md — scope/design doc for Phase C (per-occurrence consents: non-routine transportation + off-premises water). Medication is explicitly OUT (it's PR #20, separately scoped). Doc only — no code, no migration, no commit. Halt for review.
Read first (read-only): docs/pr-consents-A-scope.md, docs/pr-consents-B-scope.md (match its structure, rigor, and the "DECISIONS — RESOLVED" table format), docs/findings-operational-protective-consents-2026-05-30.md (§5d the per-occurrence options), and migration 024's acknowledgments table + index definitions. Match Phase B's doc style exactly.
Scope: two per-occurrence consents:

transportation_nonroutine_per_trip — non-routine transportation, written permission before each trip.
water_activities_off_premises_per_trip — off-premises water activity, written permission before each.

Verbatim regulatory text to cite (sourced from the rules PDF; use these exact quotes, they are no longer paraphrase):

R 400.1952(1): "A licensee shall obtain and keep on file written permission from a child's parent before a child is transported in a vehicle. Written permission must be obtained for both of the following: (a) Routine transportation, at least annually. (b) Nonroutine transportation, before each trip."
R 400.1901(1)(jj): "'Routine transportation' means regularly scheduled travel on the same day of the week, at the same time, to the same destination. Any deviation is nonroutine transportation." — this is the boundary; non-routine is the Phase C type, routine is the Phase B annual type already shipped.
R 400.1934(10): "A licensee shall obtain and keep on file written permission from a child's parent for the child's participation in either of the following: (a) Before each outdoor water activity at a swimming pool, lake, or other body of water off the child care home premises. (b) Once per season for water activities occurring on the child care home premises." — (a) is the Phase C per-occurrence type; (b) is the Phase B seasonal type already shipped.
R 400.1901(1)(yy): "'Water activities' mean activities in residential pools, lakes, ponds, or other bodies of water. Water activities do not include water play activities such as water table play, slip and slide activities, wading pools, or playing in sprinklers." — defines what counts; cite so the consent's scope is precise.

LOCKED decisions (record as resolved, fold into design body):

The unique-index problem is the central schema decision. Today acknowledgments_active_unique enforces one active row per (provider_id, type, subject_type, subject_id) WHERE archived_at IS NULL AND subject_id IS NOT NULL. Per-occurrence consents need MULTIPLE active rows per (type, child) — one per trip/outing. Resolution: relax the unique index to exempt the per-occurrence types, preserving one-active-row uniqueness for all durable types. Spell out the exact mechanism: a new partial unique index whose WHERE clause excludes the two per-occurrence types (e.g. WHERE archived_at IS NULL AND subject_id IS NOT NULL AND type NOT IN ('transportation_nonroutine_per_trip','water_activities_off_premises_per_trip')), replacing the current index. Durable Phase A/B types keep their one-active-row guarantee; per-occurrence types are exempt. The doc must show this is surgical — durable consents (field_trip, photo, transport_annual, water_seasonal) CANNOT get a second active row; only the two per-occurrence types can. Flag the migration as more delicate than B's additive column: it drops and recreates an index, so the migration must be written to not leave a window with no uniqueness enforcement (create new, then drop old, or use a transaction).
Occurrence metadata: a nullable occurrence_metadata jsonb column added to acknowledgments. Holds per-occurrence detail: trip date, destination (transport); outing date, water-body type/location (water). NULL for all durable types — purely additive, backward-compatible exactly like Phase B's expires_at. Spell out the expected JSON shape for each of the two types (suggest fields, note the app validates shape, the DB stores free jsonb). Reason for jsonb over fixed columns: transport and water have different occurrence fields, and a fixed event_date/description pair is lossy; jsonb keeps it flexible without N nullable columns.
Per-occurrence consents are DURABLE — no expires_at. "Before each trip" means one consent captured per occurrence, never renewed. A new trip is a new row. expires_at stays NULL for these types. They are NOT time-bound (that's Phase B). State this explicitly so no one wires expiry onto them.
Channel rule: same as every parent-signed consent — parent_portal / in_person_paper satisfy; provider_override alone does not. Per-occurrence types added to ENROLLMENT_CONSENT_TYPES only (mirror Phase A/B precedent — NOT PARENT_SIGNED_TYPES; that was the conflict corrected in Phase B).
Audit-state semantics — the hard part, resolve carefully. For durable consents, "pending" means "no active row." For per-occurrence consents, there is no single "is it on file" verdict — a child could have three past trip consents and need a fourth for a new trip. So the audit-state question is fundamentally different: per-occurrence types are NOT "pending/satisfied" in the enrollment-consent sense. Resolution: per-occurrence types do NOT participate in the enrollment_consents_pending / _expired verdict at all — they're event records, not enrollment-state. The doc must state how pendingEnrollmentConsentsForChild and getChildFilesAuditState exclude these types from the pending/satisfied rollup (e.g. a PER_OCCURRENCE_TYPES set the verdict skips), so adding them doesn't make every child show "pending non-routine transport" forever. This is the analog of B's expired-vs-pending care: get it wrong and every child is permanently non-compliant on a consent that's only needed when a trip actually happens.
Recording UI: provider records a per-occurrence consent when a trip/outing is planned — captures the occurrence_metadata (date, destination/water-body) plus the parent's channel. No renewal concept (each is one-and-done). Where this lives in the UI (EnrollmentConsentsModal vs. a trip-specific surface) — propose, but flag as a UX decision that can be refined; the data model is the locked part.
Does-not-foreclose / consistency with shipped work: confirm the index relaxation does not break Phase A/B durable-consent uniqueness (the whole point of the type-exclusion WHERE clause), and that occurrence_metadata jsonb is additive like expires_at.

Also include: the audit-read predicate impact (per-occurrence types excluded from the pending rollup), the backward-compat invariant (durable types untouched, new column NULL for them, index still enforces their uniqueness), the migration sketch (planning level — new partial unique index replacing the old, plus ADD COLUMN occurrence_metadata jsonb; note the index swap must avoid an uniqueness-gap window), and a "DECISIONS — RESOLVED" table at the top.
Out of scope, name explicitly: medication (PR #20, pr-20-medication-log-scope.md — its own tables/trigger/dose-log, sidesteps the index problem via distinct subject_id); three-state photo consent; the consultant policy questions.
Set Status to "Scope — FINAL, ready for build" only if every decision above is cleanly resolved in the body; if drafting surfaces a genuine conflict (especially in #5, the audit-state exclusion — that's the subtle one), halt and flag it instead of forcing FINAL. Leave the doc untracked (no git add, no commit). Report back with the resolved-decisions table and flag anything in #1 or #5 that didn't resolve cleanly.