Task: Draft docs/pr-parent-self-service-phase-y-scope.md — the build contract for Phase Y: provider-initiated medium-risk consents with templates + parent typed-signature e-signature. Doc ONLY — no code, no migration, no commit, no branch. Halt for review. This is the highest-stakes build in the arc (schema-bearing + compliance-evidence boundary + legal copy), so scope it thoroughly and recommend a sub-split so the evidence-record part can be verified before UI polish.
Authoritative parent doc: docs/pr-parent-self-service-scope.md — the Phase Y design is already decided there (read the medium-risk sections + the DECISIONS table). This doc turns that locked design into a build contract; it does NOT re-decide the design. Also read: src/lib/complianceState.js (the engine — medium-risk consents are requirements #13–17 in the registry), src/lib/acknowledgments.js (the table being extended + the channel CHECK), migration 024/025/027 (acknowledgments shape, the intake_confirm_for_parent SECURITY DEFINER RPC pattern the send/complete flow mirrors, per-occurrence consents), and migration 031 (the parent-RPC + notification patterns just shipped — child_parent_update, notification_log shape).
The locked design (build to this — do NOT re-litigate):

Provider enables medium-risk per category under the Business tab. OFF by default (the compliance safety-valve — provider decides their inspection context accepts electronic acknowledgment).
Templates managed under the Business tab: ship compliant starter templates (field trip R 400.1952(2), routine transport R 400.1952(1), water R 400.1934(10) — draft the required elements per the rules, NOT final legal copy), customizable, with a disclaimer that the provider owns final language + compliance.
Provider sends from the child's record (viewing a child ? pick a template ? send to that child's parent). Nothing reaches a parent without this deliberate provider action.
Parent completes in the portal: lands as a pending to-do (mirror intake_confirm_for_parent / the intake-reminder pattern) + a notification. Parent completes via typed-name e-signature.
The evidence record (the compliance boundary): extend acknowledgments — add channel parent_portal_esign, columns typed_signature_text, template_snapshot_text, consent_template_id (FK), and a CHECK requiring signature + snapshot present when channel = esign. Snapshot the template text at COMPLETION, not send — the completed row must show exactly what the parent signed even if the template is later edited. The completed row IS the self-contained compliance artifact (no JOIN needed to show what was agreed).
Additive: the provider paper path stays (provider can still record on paper). Enabling self-service doesn't remove it.
Excluded: religious-objection / immunization (signed official forms — stay provider-recorded + attachment).

Resolve in the doc (DECISIONS table + build contract, matching the compliance-engine Phase 1 scope's rigor):

The schema — the acknowledgments extension (exact columns, the CHECK constraint), and the new tables: consent_templates (provider's templates — provider_id, consent_type, title, body_text, enabled, timestamps, soft-delete) and consents_pending_esign (the sent-but-not-completed queue — or determine whether this can ride on the existing pending/reminder infrastructure rather than a new table; recommend). Write the migration plan (it's a real migration — Seth applies). Confirm what's new vs. extended.
The two SECURITY DEFINER RPCs (mirror migration 031's patterns): a provider-send RPC (creates the pending e-sign item for a child's parent) and a parent-complete RPC (validates parent authority, snapshots the template text into the acknowledgments row, writes the typed signature + timestamp + identity atomically). Spell out the authorization checks (the parent_family_links active-link pattern).
The template system — storage, the compliant starter elements per consent type (from the actual R 400.xxxx rules — elements, flagged that final legal copy needs Seth/consultant review, NOT CC's wording shipped as-is), the customization + disclaimer, and the snapshot-at-completion mechanic (how the body_text travels from template ? pending item ? completed acknowledgment snapshot).
The Business-tab UI — category enablement toggles + template management (list/edit/enable).
The send flow (from child's record) + the parent pending-to-do + notification flow (mirror intake-confirm).
The compliance-evidence verification — this is the strictest gate. Spell out what must be proven LIVE: (a) a completed e-sign record captures parent identity + typed signature + timestamp + the exact template text; (b) the snapshot survives a template edit — provider sends, edits the template, parent completes ? the parent's record shows what they SAW at completion, and an inspector reading the row sees the agreed text without chasing the edited template; (c) the cross-tenant/authorization boundary (a parent can only complete consents sent to their own child — same caliber as the attachments cross-tenant gate); (d) the provider-opt-in actually gates it (a parent can't self-complete a medium-risk consent for a category the provider hasn't enabled). Treat this as the hardest gate in the arc.
The sub-split recommendation — this is too big for one build+verify. Recommend a split (e.g. Y1 = schema + the evidence-record RPCs + the compliance-boundary verification, the risky part, verified alone; Y2 = templates UI + send flow + parent completion UI, the surface work, on top of verified Y1). Recommend the sequence and each sub-phase's hard gate.
The compliance-engine connection — note that once medium-risk consents are e-signable, requirements #13–17 in the engine resolve from these records; confirm the engine reads them without change (the parent_portal_esign channel should satisfy the same way parent_portal does — check the PARENT_SIGNED_SATISFYING_CHANNELS set; flag if it needs the new channel added).

Deliverable: the decisions table, the schema/migration plan, the two RPCs' contracts, the template-starter elements (flagged for human legal review), the flows, the strict compliance-evidence verification gate, the sub-split recommendation, and the engine-connection check. Status "Scope — DRAFT for review" (the schema details, the RPC contracts, and the sub-split are calls Seth reviews; the template legal copy is explicitly flagged as needing human/consultant sign-off, not CC's draft). Leave untracked. Halt with the decisions + flagged questions — especially the schema, the snapshot-at-completion mechanic, and the sub-split.


Task: BUILD Phase Y1 — the compliance-evidence data layer for medium-risk consent e-signature: schema + the three SECURITY DEFINER RPCs + the WORM evidence record. Per docs/pr-parent-self-service-phase-y-scope.md (Y1 section). NO UI (that's Y2). Branch off main, build, push, halt. Migration written but NOT applied (Seth applies). Do NOT merge — Y1 has an 8-step live verification gate that must pass first.
Authoritative spec: docs/pr-parent-self-service-phase-y-scope.md — build the Y1 portion verbatim. Read it fully. Seth has approved: schema as scoped (§2), exact-match stale-read via IS DISTINCT FROM (§3b), the Y1/Y2 split, the WORM trigger (decision 14), and CC's minor recommendations. Template wording is NOT CC's to finalize — use the compliance-required elements from §4 as the starter template bodies, but mark clearly (code comment + a note in the migration) that the body text is placeholder pending Seth/consultant legal review. Read first: src/lib/acknowledgments.js (the channel CHECK + PARENT_SIGNED_SATISFYING_CHANNELS), migration 025 (intake_confirm_for_parent — the RPC auth pattern to mirror), migration 031 (the RPC + notification_log patterns just shipped), and src/lib/complianceState.js + src/lib/medication.js (the two duplicated copies of PARENT_SIGNED_SATISFYING_CHANNELS).
Branch: git checkout main, git pull --ff-only origin main, create feature/parent-self-service-phase-y1-evidence-boundary. Confirm before editing. Migration is 033 (032 was deleted on the Phase X branch — confirm the migrations folder ends at 031 before numbering).
Build — Y1 only:
1. Migration 033 (written, NOT applied):

Extend acknowledgments: add typed_signature_text text, template_snapshot_text text, consent_template_id uuid REFERENCES consent_templates(id) ON DELETE SET NULL. Expand the channel CHECK to include parent_portal_esign. Add chk_acknowledgments_esign_shape: signature + snapshot non-null when channel = parent_portal_esign, both NULL otherwise.
New table consent_templates: per-provider templates — provider_id, consent_type, title, body_text, enabled, soft-delete (archived_at), timestamps. Partial-unique on (provider_id, consent_type) WHERE archived_at IS NULL (one active template per category). Seed the starter bodies from §4's required elements (placeholder copy, flagged for legal review).
New table consents_pending_esign: the sent-but-not-completed queue — provider_id, child_id, parent_id, consent_type, consent_template_id, template_body_at_send text (the stable text the parent reads), per_send_metadata jsonb (per-trip data), sent_at, resolved_at, resolved_via, soft-delete.
profiles.medium_risk_consents_enabled jsonb — server-authoritative opt-in gate, defaults all five categories to false (OFF by default).
WORM trigger block_esign_evidence_update_trg: blocks UPDATE to typed_signature_text, template_snapshot_text, consent_template_id after initial write (write-once evidence). archived_at stays mutable (provider can archive). Mirror the block_parent_archive trigger shape from 031.
RLS on the two new tables: provider-scoped for templates; parent can read/complete only their own pending items (mirror the parent_family_links active-link pattern). No parent INSERT on pending (provider-send only). Spell out each policy.
Idempotent (if exists/if not exists/or replace). Header carries the verification SQL Seth runs after applying.

2. The three SECURITY DEFINER RPCs (mirror intake_confirm_for_parent's auth shape):

consent_esign_send(child_id, consent_type, per_send_metadata) — provider-only; checks the provider's medium_risk_consents_enabled gate for that category (server-authoritative — reject if OFF); reads the CURRENT consent_templates.body_text, writes a consents_pending_esign row with template_body_at_send = current body; writes a notification_log row for the parent. Validates the caller owns the child.
consent_esign_complete(pending_id, typed_signature_text, claimed_body_text) — parent-only; validates the caller is the active parent for the pending item's child; re-reads the CURRENT template body server-side and compares to claimed_body_text via IS DISTINCT FROM (exact match) — if different, raise template_changed_since_send (the parent must re-read); if match, atomically insert the acknowledgments row with channel parent_portal_esign, typed_signature_text, template_snapshot_text = the confirmed body, consent_template_id, and mark the pending row resolved. This is the evidence-write — get the atomicity right (insert + resolve in one transaction).
consent_esign_rescind(pending_id) — provider-only; marks a pending (not-yet-completed) item resolved/cancelled. (Per the scope; V1 rescind of a completed consent is out of scope — provider amends.)
All three: revoke all from public; grant execute to authenticated.

3. Update PARENT_SIGNED_SATISFYING_CHANNELS — all THREE in-tree copies in lockstep: src/lib/childFiles.js (source), src/lib/complianceState.js (duplicate), src/lib/medication.js (duplicate). Add 'parent_portal_esign' to each. The backward-compat test in complianceState.test.js locks the duplication invariant — confirm it still passes (and update it if it asserts the exact channel set). This is what makes the Phase 1 engine treat e-signed rows as on_file for requirements #13–17 with no registry change.
Constraints: Y1 is data-layer ONLY — NO UI (no Business-tab toggles, no template editor, no send modal, no parent card — all Y2). Migration written, NOT applied. Everything OFF-by-default so Y1 merges dormant. Don't touch untracked junk.
Tests: the RPC logic where testable, the channel-set update, the shape CHECK logic, the engine treating parent_portal_esign as satisfying. Build clean + full suite green (1201 + new, no regression).
Commit + push, halt. Do NOT merge. Commit msg: feat(parent-self-service): Phase Y1 — medium-risk e-sign evidence layer (schema + RPCs + WORM, zero UI).
The 8-step live verification gate (HARD — no merge until all pass; run by Seth after applying migration 033, against real auth/fixtures — Jeff/2549scio, klsnay/Audrey, Dominique). This is the compliance-evidence boundary, same caliber as the attachments cross-tenant gate. Report the exact steps + SQL/console for each:

Migration applied + schema-confirm SQL (tables, columns, CHECK, trigger, RPCs exist).
Provider enables a category + creates a template (via SQL/RPC, since no UI yet).
Provider sends to parent A via consent_esign_send ? pending row + notification_log written.
Provider edits the template between send and completion.
Parent A completes via consent_esign_complete ? confirms (a) the stale-read check FIRES (raises template_changed_since_send because the template changed), then after re-read (b) successful completion writes the authoritative snapshot.
Snapshot survives a later template edit — after completion, edit/delete the template; the completed acknowledgments row still shows exactly what parent A signed (no JOIN, no chase).
Cross-tenant denial — parent A attempts consent_esign_complete on a pending sent to parent B's child ? DENIED.
Opt-in bypass — a parent attempts consent_esign_send directly (not provider) ? DENIED; and a send for a category the provider has OFF ? DENIED server-side.
Plus invariant checks: the WORM trigger blocks an UPDATE to the evidence columns; provider archival of a completed row still works.

Gate: if step 6, 7, or 8 fails, the evidence boundary has a hole — halt, do NOT merge, report. These three are the compliance-defensibility proof.
Report: what was built, the migration 033 SQL for Seth to apply, confirmation the three PARENT_SIGNED_SATISFYING_CHANNELS copies are in lockstep, and the exact step-by-step for the 8-step gate (the SQL/RPC calls Seth runs as each actor).


Task: Fix a bug in consent_esign_send (migration 033, applied) found during the Y1 live gate. The RPC fails with null value in column "recipient_id" of relation "notification_log" violates not-null constraint (code 23502) — it inserts the parent-notification row with recipient_id => null, but the real notification_log.recipient_id is NOT NULL. The whole transaction rolls back, so no pending row is created. Investigate the real notification_log schema + how existing writers populate recipient_id for parent-recipient rows, fix the RPC to match, ship as migration 034 (CREATE OR REPLACE the function). Build on the Y1 branch, push, halt. Do NOT merge. Do NOT apply (Seth applies).
Confirmed via live gate (provider auth'd as 35a6d4dd, calling consent_esign_send for Aleshia): the RPC errors 23502 on the notification_log insert because recipient_id is null. The failing row shows recipient_type='parent', recipient_id=null. The pending-row insert + notification insert are one transaction, so the whole thing rolls back (this is why consents_pending_esign had 0 rows after the call).
Investigate FIRST (don't assume):

Read the REAL notification_log schema — which columns are NOT NULL? (CC's RPC assumed recipient_id and recipient_email were nullable; at least recipient_id is not.) Check the migration that created notification_log.
Read how the EXISTING notification writers populate a parent-recipient row — api/notify-state-change.js, api/cron-dispatch-reminders.js, and the child_parent_update RPC from migration 031 (which also writes parent... actually it writes provider-recipient — check). Find an existing example of a row where recipient_type='parent': what goes in recipient_id? A specific parent's user id? How is "the parent" resolved when a child can have multiple linked parents?
Resolve the design question: for a consent sent to a child, who is the notification recipient? If a child has multiple active parents via parent_family_links, does the existing pattern write one row per parent, or pick one, or use a different recipient model? Match whatever the existing parent-notification pattern does — do not invent a new one.

Then fix: correct the consent_esign_send notification insert to populate recipient_id (and any other NOT NULL column it's missing) per the established pattern. If the child has multiple parents and the pattern is one-row-per-parent, loop. Ship as migration 034 that CREATE OR REPLACEs consent_esign_send (the function can be replaced in place; no need to touch the tables).
Also check the OTHER two RPCs for the same bug: consent_esign_complete and consent_esign_rescind also insert into notification_log (provider-recipient rows). Do THOSE populate recipient_id correctly, or do they have the same null bug? consent_esign_complete writes a recipient_type='provider' row with recipient_id => v_provider_id (looks populated) — but verify all three RPCs' notification inserts against the real NOT NULL columns so we don't hit the next one mid-gate. Fix all that are broken in migration 034.
Constraints: migration 034 (CREATE OR REPLACE functions only — no table changes unless a column genuinely must change). Written, NOT applied. Don't touch untracked junk.
Verify: build clean, tests green. Report: the real notification_log NOT NULL columns, how existing parent-notifications populate recipient_id, what you changed in each RPC, and migration 034 for Seth to apply.
Report the root cause first, then the fix.